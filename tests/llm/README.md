# LLM Test Suite

This directory contains automated tests for the LLM pipeline.

## Structure
- `provenance_tests/`: Verify metadata extraction and source linking.
- `safety_tests/`: Adversarial prompts to test safety filters.
- `calibration_tests/`: Benchmarks for model accuracy.

## Running Tests
Run `npm run test:llm` (script to be configured).
